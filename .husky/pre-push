set -e

./scripts/guard-no-bun.sh || exit 1
echo "Running lint checks. Don't expect me to be nice about it."
npm run lint || {
  echo "❌ Of course they failed. Go fix the errors above. Baka."
  exit 1
}

echo "Running type checks. Try not to mess this up."
npm run type-check || {
  echo "❌ Type checks failed. Fix them. It's not like I'm waiting or anything."
  exit 1
}

echo "✅ ...All passed. Not that I was worried. Idiot."
